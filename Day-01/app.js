// Import the `cat-me` package (installed via npm).
// `catMe` is a function that returns a random ASCII cat string each time it's called.
const catMe = require("cat-me");

// Call the function and print the returned ASCII cat to the console.
// Useful to verify the package is installed and working.
console.log(catMe());